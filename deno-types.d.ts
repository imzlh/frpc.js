// Deno type supplements for node:* modules compatibility
// Buffer is already available globally in Deno when using node:* modules
// No additional declarations needed — Deno provides them automatically
export {};
